{
  "name": "nodejs-news",
  "version": "1.0.0",
  "description": "A Node.js Express app for news",
  "main": "api/app.js",  // Points to app.js inside the /api folder
  "scripts": {
    "start": "node api/app.js",  // Run app.js for production on Vercel
    "dev": "nodemon api/app.js",  // For local development
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": ">=0.21.1",
    "body-parser": "^1.19.0",
    "ejs": "^3.1.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
